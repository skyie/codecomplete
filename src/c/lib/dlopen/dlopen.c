#include "common.h"


/*


#include <dlfcn.h>
void* dlopen(const char* libfilename, int flags);
        return handle on success,-1 error
flags:


const char* dlerror();
    出错返回错误的字符串指针，没错误返回的NULL


void* dlsym(void* handle, void* symbol);    
    返回symbol地址，没找到symbol返回NULL
    

关闭共享库
int dlclose(void* handle);
    return 0 success, -1 error




*/




















